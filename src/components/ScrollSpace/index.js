import React from 'react'

import styles from './ScrollSpace.module.scss'

export default function ScrollSpace(props) {
    return(
        <div className={styles.ScrollSpace} id="scrollSpace">
        </div>
    )
}